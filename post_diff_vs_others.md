# SafeShrink 和其他 Skill 有什么区别？

> 一句话：**它不是"又一个文档处理 Skill"，它是你本地的文档隐私守门员。**

---

## 大多数 Skill 在做什么

SkillHub 上 90% 的文档类 Skill 都是同一个模式：

```
用户上传文件 → Skill 读取 → 调用 API 处理 → 返回结果
```

**问题出在哪？**

| 环节 | 风险 |
|------|------|
| 文件上传 | 你的合同、财务表、客户名单，先传到 Skill 的服务器 |
| 云端处理 | 第三方模型/服务看到你的数据 |
| 结果返回 | 处理后的文件再次经过网络传输 |

你用的是"效率工具"，但代价是**让渡了隐私**。

---

## SafeShrink 的不同

### 1. 本地处理，零上传

SafeShrink 是**桌面应用**，所有压缩、脱敏、转换都在本地完成。文件不会离开你的电脑。

> 你在用 AI 处理文档时，SafeShrink 是唯一一个**不需要把文件传给任何人**的工具。

### 2. 主动拦截，不是被动等待

大多数 Skill 要等你**主动调用**才会工作。

SafeShrink 被集成到 AI Agent 的工作流中——当你准备把一份敏感文档发给 AI 时，它会**主动跳出来说**："这份文件要不要先优化一下再发？"

**从"你找工具"变成"工具找你"。**

### 3. 三种压缩模式，不是"压缩一下"

| 模式 | 适用场景 |
|------|----------|
| **标准压缩** | 日常分享，保留原格式，压缩率适中 |
| **激进压缩** | 需要极致瘦身，可接受格式变化 |
| **深度清理** | 预览模式，手动确认后再保存 |

这不是简单的"压缩一下"，而是**按场景选择策略**。

### 4. SSD 格式：自有文档格式

PDF 转 Markdown 不是 SafeShrink 的独创，但 SSD（SafeShrink Document）格式是：

- 保留 OCR 识别结果
- 保留原始布局信息
- 可逆转换（SSD ↔ 原文档）

**不是简单的格式转换，而是带元数据的中间格式。**

### 5. 批量处理，不是单文件

一次拖入 20 个文件，自动按类型分流处理。其他 Skill 大多只支持单文件。

---

## 一张图看懂区别

```
                    传统 Skill          SafeShrink
                    ─────────          ──────────
文件去向            云端/第三方          本地 ✅
处理触发            被动等待             主动拦截 ✅
压缩策略            单一                 三种模式 ✅
文件格式            转成通用格式          自有 SSD 格式 ✅
批量处理            大多不支持            支持 ✅
隐私保护            依赖服务方承诺        本地完成，零信任 ✅
```

---

## 什么时候用 SafeShrink？

- ✅ 要把合同/财务表发给 AI → **先用 SafeShrink 脱敏**
- ✅ 文档太大 AI 处理不了 → **先用 SafeShrink 压缩**
- ✅ 批量处理一堆文档 → **SafeShrink 批量模式**
- ✅ 需要保留文档结构的可逆转换 → **SSD 格式**

## 什么时候不用？

- ❌ 只是简单转个 PDF 为文本 → 用普通 Skill 就够了
- ❌ 文件完全不敏感、不在意隐私 → 随便选
- ❌ 需要在线协作编辑 → SafeShrink 不是编辑器

---

## 总结

**SafeShrink 不是在"文档处理"赛道上卷功能，而是在"隐私保护"赛道上卷信任。**

其他 Skill 帮你**处理**文档，SafeShrink 帮你**决定**文档该不该被处理、怎么处理、处理到哪里为止。

---

*SafeShrink v1.2.1 | 本地处理 · 隐私优先 · 主动拦截*
